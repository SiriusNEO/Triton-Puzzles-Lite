[project]
name = "triton-puzzles-lite"
version = "0.1.0"
description = "A lightweight script-based version of Triton Puzzles."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy<2",
    "torch==2.5.0",
]

[tool.uv]
package = false
